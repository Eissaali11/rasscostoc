# ERP-006A — PR-006A-6 Technical Documentation

**Status:** Implemented — **STOP** before PR-006A-7 (Review UI)  
**Date:** 2026-07-14  
**Package:** `@stockpro/ai-extraction`

## Approval

```text
PR-006A-6 = Approved
Architecture Sign-off: APPROVED
START PR-006A-6
```

## Scope delivered

| Component | Implementation |
|-----------|----------------|
| Matching Runtime | `TechnicianMatchingRuntime` |
| Cascade Matcher | `runCascadeSearch` (SN→SIM→TID→Mobile→Merchant→Incident→Branch→City) |
| Ranking | `rankCandidates` (deterministic) |
| Explainability | `buildExplanation` / `matched_reason[]` (Arabic labels, no raw weights) |
| Confidence | `evidence_score`, `confidence`, bands auto/review/manual |
| Ambiguity | `detectAmbiguity` |
| Provenance | `matching_engine_v1` + session/attempt/graph versions |
| Repository ports | Technician / Request / Execution / Merchant / Branch |
| InMemory + Fixtures | `createMatchingFixturePorts` |
| Port adapter | `GraphMatchingEngine` → `MatchResult` |

## Pipeline

```text
Canonical Device Graph
        ↓
Matching Runtime
        ↓
Candidate Ranking
        ↓
Explainability
```

Read-only. No Courier writes. No Apply. No Review UI.

## Confidence bands

| Band | Range |
|------|--------|
| auto_match_candidate | 95–100 |
| recommended_review | 80–94 |
| manual_review_required | &lt; 80 |

Ambiguity forces demotion from auto → recommended_review (never auto-resolves).

## Forbidden (honored)

Review UI · Apply · Courier · FSM · Business Rules · live Gemini wiring · `ocr.helper` · `/courier/pdf/:id` · Queue/Redis/Drive · DB migrations · production flags ON

## Tests

```bash
npm run test:unit -- packages/ai-extraction
```

## Official status after this PR

```text
PR-006A-1…6 = Implemented ✅

STOP
Do not start PR-006A-7 (Review UI Runtime)
until Architecture approval is explicitly recorded.
```

**Next (when approved):** Review UI → Business Rules → **AI Engine Consumer Adapter — Courier** → Provider settings + Pilot.  
Engine remains a **multi-provider platform**; Courier is only the first consumer — [platform model](./ERP-006A-platform-consumer-model.md).
