# ERP-006A — PR-006A-4 Technical Documentation

**Status:** Implemented — **STOP** before PR-006A-5  
**Date:** 2026-07-14  
**Package:** `@stockpro/ai-extraction`

## Approval

```text
PR-006A-4 = Approved
Architecture Sign-off: APPROVED
START PR-006A-4
```

## Scope delivered

| Item | Implementation |
|------|----------------|
| Vision Port | `VisionProvider` / `VisionProviderInput` |
| Gemini Adapter | `GeminiVisionAdapter` (Structured Output `responseMimeType: application/json`) |
| HTTP client | `FetchGeminiHttpClient` (injectable) |
| Fixture provider | `FixtureVisionProvider` (unit tests, no network) |
| JSON Schema | `INSTALLATION_DEVICE_SCHEMA_V3` + `validateAgainstSchema` |
| Per-field confidence | `extractConfidenceFields` |
| Multi-device | `VisionExtractionService.extractAll` (one Vision call per `device_id`) |
| Prompts | `buildDeviceExtractionPrompt` |

## Hard constraints (honored)

- No Courier / `/courier/pdf/:id` / `ocr.helper` / FSM / Review UI / Apply  
- `AI_EXTRACTION_FEATURE_FLAG.enabled === false`  
- `vision_live_production === false`  
- `GeminiVisionAdapter` default `allowLive=false` — no network unless explicitly enabled in non-prod tooling  

## Tests

```bash
npm run test:unit -- packages/ai-extraction
```

## Official status after this PR

```text
PR-006A-1 = Approved ✅
PR-006A-2 = Implemented ✅
PR-006A-3 = Implemented ✅
PR-006A-4 = Implemented ✅

STOP
Do not start PR-006A-5 until Architecture approval is recorded.
```

PR-006A-5 (when approved): Relationship Engine → Graph runtime enrichment from Vision fields — still isolated.
