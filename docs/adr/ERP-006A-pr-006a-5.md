# ERP-006A — PR-006A-5 Technical Documentation

**Status:** Implemented — **STOP** before PR-006A-6  
**Date:** 2026-07-14  
**Package:** `@stockpro/ai-extraction`

## Approval

```text
PR-006A-5 = Approved
Architecture Sign-off: APPROVED
START PR-006A-5
```

## Scope delivered

| Item | Implementation |
|------|----------------|
| Vision → Graph | `CanonicalDeviceGraphBuilder` |
| Relationship Engine | `RelationshipEngine` (SN↔SIM↔TID↔Merchant↔Branch) |
| Conflicts / duplicates | `conflicts_with` edges + issues |
| Merge Rules | `evaluateMergeRules` — **never auto-applies** (`applied: false`) |
| Matching prep | `prepareForMatching` stubs (`awaiting_matching_engine`) |
| Provenance | `graph_runtime_v1` + `extraction_attempt_id` on graph/edges |

## Policy

> On doubt: do **not** merge — keep separate devices + Review.

## Out of scope (honored)

Review UI · Apply · Courier · FSM · live Technician Matching · Business Rules runtime · production enablement · `/courier/pdf/:id` · `ocr.helper.ts`

## Tests

```bash
npm run test:unit -- packages/ai-extraction
```

## Official status after this PR

```text
PR-006A-1…5 = Done ✅

Current Status: STOP
Awaiting Architecture approval before PR-006A-6.

Next (when approved): Technician Matching Runtime
  — cascade SN→SIM→TID→Mobile→Merchant→Incident→Branch
  — ranking + matched_reason[] + confidence + candidates
  — read-only; no Apply / Review UI / Courier writes / production
```
