# ERP-006A — PR-006A-3 Technical Documentation

**Status:** Implemented — **STOP** before PR-006A-4 (Vision)  
**Date:** 2026-07-14  
**Package:** `@stockpro/ai-extraction`

## Approval

```text
PR-006A-3 = Approved
Architecture Sign-off: APPROVED
START PR-006A-3
```

## Scope delivered

| Component | Implementation |
|-----------|----------------|
| PDF Renderer port | `PdfRenderer` + `FixturePdfRenderer` + `PassthroughPageRenderer` |
| Page Splitter | `ContiguousPageSplitter` |
| Image Detection | `FullPageImageDetector` (full-page regions; extensible) |
| Quality Analyzer | `HeuristicQualityAnalyzer` → per-image `quality_score` 0–100 |
| Image Preprocessing | `ProfileImagePreprocessor` (profiles + recorded ops) |
| Stable Image IDs | `img:{document_id}:p{page}:r{region}` |
| Provenance | `docproc_v1` on document + each image |
| Pipeline | `DocumentProcessingPipeline` |
| Heuristic Grouping | `HeuristicGroupingProvider` (per ERP-006A-1 PR sequence) |

## Explicit non-goals (this PR)

- Live Gemini / Vision extraction  
- Courier `/courier/pdf/:id` / `ocr.helper.ts`  
- Native production PDF raster engine (fixture / pre-rendered pages for now)  
- Production feature flag ON  
- Matching / Review UI / Apply  

## Native PDF note

`FixturePdfRenderer` supports JSON page fixtures, single images, and pre-rendered pages. A production raster adapter (e.g. pdf.js + canvas) is deferred to a later approved PR — does **not** unblock Grouping/Quality contracts.

## Tests

```bash
npm run test:unit -- packages/ai-extraction
```

## Next gate (official)

```text
Current Status: STOP
Awaiting Architecture approval before PR-006A-4.

PR-006A-4 scope (when approved): Vision Extraction layer only
  — Provider Adapter, Vision Port, JSON Extraction, Structured Output,
    Schema Validation, per-field confidence, multi-device support
  — Still isolated: no Courier, no Review UI, no Apply, no production enablement
```