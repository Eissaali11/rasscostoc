# ERP-006A PR-006A-9 — Courier PDF Value Path (Slice 1)

**Status:** **In Progress — Slice 1 implemented**  
**Date:** 2026-07-14  
**Product contract:** [ERP-006A-courier-pdf-primary-ux.md](./ERP-006A-courier-pdf-primary-ux.md)

## What Slice 1 delivers

Daily path stays on `/courier/pdf` + `/courier/pdf/:id`:

```text
Upload → OCR normalize → devices[] cards
      → serial-lookup match (technician)
      → POST /api/courier/pdf/:id/complete
      → saveExecution + CompletionGuard + custody workflow
```

| Surface | Role |
|---------|------|
| `/courier/pdf` | Primary work UI |
| AI Engine | Hook only (`runAiEngineExtraction` returns null while flag off) |
| `/ai-review` | Internal QA — unchanged |

## Artifacts

| Piece | Path |
|-------|------|
| Adapter | `apps/api/src/modules/courier/application/ai-engine/courier-pdf-extraction.adapter.ts` |
| Upload normalize | `CourierService.uploadPdfReport` |
| Complete API | `POST /api/courier/pdf/:id/complete` → `completePdfReport` |
| Review UI | `apps/portal/src/pages/courier/courier-pdf-review.tsx` |
| Tests | `courier-pdf-extraction.adapter.test.ts` |

## Explicit non-goals (still)

- Live Gemini / `AI_EXTRACTION_FEATURE_FLAG.enabled = true`
- Full Business Rules runtime (PR-006A-8)
- Making `/ai-review` the daily path
- Multi-request split from one PDF

## Next

- Slice 2: wire `@stockpro/ai-extraction` pipeline when flag + ERP-003 allow  
- Optional: Architecture approval for PR-006A-8 Business Rules feeding card statuses  

```text
PR-006A-9 Slice 1 = Implemented
Production AI flag = OFF
START live engine Slice 2 = NOT issued
```
