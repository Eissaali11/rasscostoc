# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment variables (IMPORTANT: Never commit!)
.env
.env.local
.env.production

# AI Engine secrets (API keys encrypted at rest)
data/ai-engine-settings.json

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
logs/
*.log
npm-debug.log*

# Temp files
server/public
vite.config.ts.*
*.tar.gz

# Backups
*.sql
!migrations/**/*.sql
*.backup
backups/
# Mobile apps (separate repos)
mobile/
technician-app/

# VS Code local history
.history/

# Expo
app.json
eas.json
coverage/
.env.ops

# Phase B1.3: Playwright generated output — never commit run artifacts.
playwright-report/
test-results/

# Zero Local Storage: runtime PDF directories must never be tracked in git.
# The application creates these directories on demand (mkdirSync recursive)
# when/if it writes to them, so no placeholder file is required to keep
# them present.
/uploads/pdf/**
/uploads/courier-pdf/**
/apps/api/uploads/pdf/**
/uploads/test-files/*.pdf

# Untracked (not deleted) from git: suspected to contain real
# transfer-record data, unreferenced by any functional code. Preserved in
# git history at commit 0776825 and after. Exact path only, no wildcard.
/attached_assets/transfer_2026-01-28_cab0b9c0_1769590127519.pdf

# SDUI Keys and Operational Configs (Security Rules)
/config/*.key
/config/*.pem
/config/mobile-sdui-filters.json
!/config/mobile-sdui-filters.json.example

