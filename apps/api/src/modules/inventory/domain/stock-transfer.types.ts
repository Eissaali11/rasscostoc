export type TransferPackagingType = 'box' | 'unit';

export type TechnicianInventoryBalance = {
  boxes: number;
  units: number;
};

export type TransferInventoryType = 'warehouse' | 'technician' | 'moving' | 'fixed';
